Tree-shaking fixture used by `scripts/measure-tree-shaking.mjs` to track the
size of a minimal `Button`-only consumer over time. Not published to npm; the
`files` allowlist in `package.json` excludes this directory.
